# プライバシー・データ取り扱い・安全性

[English](SAFETY.md) · 日本語

会議音声は、企業が日常的に生成するデータの中でも特に機微なものです。人事の議論、
未公表の財務情報、顧客名、そして「録音が始まっていると気づく前に言ってしまったこと」が
含まれます。したがって音声プロダクトは、まずデータ取り扱いのプロダクトであり、
機械学習のプロダクトであるのはその次です。

本書は koe が実際にそのデータをどう扱うかを、**コードに基づいて**記述します。
努力目標ではありません。

---

## 1. システムが触れるデータ

| データ | 保持場所 | 保持期間 |
|---|---|---|
| 生音声（PCM16） | プロセスメモリのみ | 発話の終端検出ごとに解放 |
| 中間仮説 | プロセスメモリ、接続中クライアントへ送信 | 次の中間結果で破棄 |
| 確定文字起こし | プロセスメモリ、クライアントへ送信 | セッション破棄まで |
| 議事録 | プロセスメモリ、クライアントへ送信 | レスポンス送出まで |
| コスト台帳 | プロセスメモリ、上限付きリングバッファ | プロセス再起動まで |
| ログ | 標準出力 | 運用側のログ保持設定に従う |

### コードで確認済みの性質

以下は慣習ではなく、実装とテストによって担保されています。

- **音声はディスクに書き込まれません。** 音声経路上にファイル書き込みはありません。
  パッケージ内の書き込み処理は評価データセットと CLI レポート出力のみで、
  いずれも明示的に呼び出した場合に限られます。
- **音声は発話終端ごとに解放されます。** `StreamingSession` は確定した発話の
  バッファを削除し、セッション全体を蓄積しません。本来はメモリ対策
  （同時通話1時間あたり約115MB）ですが、副次的に、クラッシュダンプに含まれる音声が
  会議全体ではなく最大1発話に限定されます。
  テスト: `test_audio_is_released_after_each_utterance`
- **文字起こしの内容はログに出力されません。** ログにはセッションID、プロバイダ名、
  所要時間、コスト、エラー種別のみが記録されます。認識テキスト、議事録の内容、
  話者名は含まれません。したがってログ基盤が会議の第二の複製にはなりません。
- **コスト台帳はメタデータのみを保持します。** セッションID、プロバイダ、モデル、
  時間、トークン数、コスト。内容は含みません（`telemetry/ledger.py::Entry`）。

### 提供していないもの

「無い」ことを「保証されている」と誤解されないよう明記します。

- **保存時暗号化はありません。** 保存していないためです。永続化を追加する場合、
  それは実装者の責任範囲になります。
- **認証・認可はありません。** API は無認証です。自前のゲートウェイの背後に置く
  前提で、Terraform も ALB 経由でプライベートサブネットに配置しますが、
  **koe 自身は呼び出し元を認識しません**。
- **課金以上のテナント分離はありません。** 台帳はテナント別に支出を分けますが、
  データを分離しません。1プロセスが接続してきた相手すべてを処理します。
- **監査ログはありません。** 誰が何にアクセスしたかの改竄検知可能な記録は存在しません。

---

## 2. データを見る第三者

自分で運用しないモデルを呼ぶことがハーネスの目的である以上、ここが最も重要です。

| プロバイダ | 受け取るもの | 設定箇所 |
|---|---|---|
| 音声認識 | 生音声（1発話単位） | `asr` サービス登録 |
| LLM | 要約のため**文字起こし全文** | `llm` サービス登録 |
| 話者分離 | 生音声 | `diarizer` サービス登録 |

明示すべき帰結が2つあります。

**LLM は会議全体を見ます。** 議事録生成には全文が必要だからです。社外に出せない
会議がある場合、制御すべき境界は音声認識段ではなく **LLM 段**です。

**既定設定では外部を一切呼びません。** 認証情報が無い場合、koe は完全に
プロセス内の決定論的モックで動作し、ネットワーク通信を行いません
（`Settings.use_mocks`）。外部呼び出しはすべて、利用者が設定した認証情報の結果です。

### プロバイダ側の保持

プロバイダが何を保持するかを koe は制御できません。契約条件を確認してください。

- Anthropic・OpenAI はいずれも API トラフィックのゼロリテンション設定を提供して
  いますが、標準アカウントの既定値ではありません。
- 音声を自社ネットワークから出せない要件がある場合、ローカル音声認識
  （`asr` エクストラの `faster-whisper`）と自社ホストの LLM を利用してください。
  プロバイダプロトコルにより、これは登録の変更であってコードの変更ではありません。

---

## 3. 個人情報保護法（APPI）における位置づけ

日本国内で運用する場合、会議音声と文字起こしは、参加者が識別可能になった時点で
**個人情報**に該当します。話者分離は構造上それを真にし、議事録は氏名によって
さらに明確にします。

koe の動作から直接導かれる論点は以下のとおりです。

- **話者ラベルは個人情報です。** 話者分離の出力は発話を個人に帰属させます。
  議事録はさらに、氏名を約束事項に結び付けます。
- **海外プロバイダへの音声送信は越境移転（第28条）です。** 米国ホストの音声認識・
  LLM を利用する場合、同意または相当措置の確認といった適切な根拠が必要です。
  Terraform の既定リージョンは `ap-northeast-1` ですが、これは**サービス**の
  配置であり、第三者モデルの実行場所を変えるものではありません。
- **利用目的の特定。** 議事録作成のために取得した文字起こしを、黙って学習データに
  転用することはできません。koe は登録されたプロバイダ以外にデータを送らず、
  二次利用のために保持もしません。
- **録音の同意取得は実装していません。** 全参加者の同意取得は本レイヤーより上位の
  プロダクト判断です。日本では実務上、会議冒頭での明示的なアナウンスが慣行です。

本節はシステムの挙動の説明であり、法的助言ではありません。義務の確認は
法務担当にご相談ください。

---

## 4. データ最小化のための設定

露出を減らす設定を、効果の大きい順に示します。

```bash
# 完全にローカル動作。外部通信を一切行わない。
KOE_FORCE_MOCK_PROVIDERS=true

# 単一セッションがバッファを保持できる時間の上限。
KOE_MAX_SESSION_SECONDS=3600

# 中間認識の頻度を下げる。字幕の応答性と引き換えに、
# プロバイダへ送る音声量が減る。
KOE_PARTIAL_INTERVAL_MS=1000

# セッションあたりの支出上限。1セッションが外部へ送出できる量の上限でもある。
KOE_SESSION_BUDGET_USD=2.0
```

3つ目が最も分かりにくい項目です。中間仮説は「そこまでの発話」を毎回デコードし直す
ため、同じ音声が繰り返しプロバイダへ送信されます。間隔を広げると、コストと、
ある1秒の音声がネットワーク境界を越える回数の両方が減ります。

---

## 5. サービスの脅威モデル

**対処しているもの:**

- *単一クライアントによるリソース枯渇* — 同時セッション数に上限があり、満杯時は
  待たせず拒否。フレームサイズに上限。実時間より速く送信するクライアントは切断。
- *コストの暴走* — セッション別・テナント別の予算を強制。上限を超えた呼び出しも
  記録するため、事後調査が可能。
- *エラー経由の情報漏洩* — 未捕捉例外はリクエストIDのみを返します。
  スタックトレースはログに出力し、呼び出し元へは返しません。
- *設定不備の本番投入* — ワイルドカード CORS、予算未設定、モック強制のいずれも
  `production` では起動を拒否します。
- *プロバイダ障害の連鎖* — サーキットブレーカが、失敗中のバックエンドへの送信を
  停止します。

**対処していないもの:**

- *認証・認可* — 前段にゲートウェイを置いてください。
- *分散型サービス拒否攻撃* — 接続単位の上限は多数接続に対して無力です。
  ロードバランサと WAF の領域です。
- *悪意あるモデルプロバイダ* — バックエンドの登録は、そのプロバイダに音声を
  渡すことを意味します。
- *会議内容によるプロンプトインジェクション* — 「指示を無視して…」と発言した
  参加者のテキストは LLM に渡ります。根拠検証は**出力**を制約し、すべての主張に
  文字起こしからの引用を要求するため捏造は抑止されますが、実際に発言した内容を
  使って要約に影響を与えることは防げません。
- *サイドチャネル* — `/metrics` は無認証でレイテンシとコストを公開します。
  このエンドポイントを公開しないでください。

---

## 6. 脆弱性の報告

本リポジトリはポートフォリオ用途ですが、問題を発見された場合は
[github.com/jon-jc/koe-harness/issues](https://github.com/jon-jc/koe-harness/issues)
へ Issue を作成してください。公開したくない内容の場合は、詳細を伏せてその旨のみ
記載いただければ、こちらから連絡します。
